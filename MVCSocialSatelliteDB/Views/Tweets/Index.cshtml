@model IEnumerable<MVCSocialSatelliteDB.Models.Tweet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsPosted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeToPost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsPosted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeToPost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TweetID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TweetID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TweetID })
        </td>
    </tr>
}

</table>
